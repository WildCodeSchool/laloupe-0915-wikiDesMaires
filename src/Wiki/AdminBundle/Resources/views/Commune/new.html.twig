{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Commune creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('commune') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
